# Static Viewer

Open:

```text
docs/viewer.html
```

The viewer accepts analyzer JSON output and renders classification, explanation, graph lines, feedback paths, proof obligations, identity-resolution counts, and the validity-scope warning.

It is static HTML/JavaScript and does not call external APIs.

The viewer is for review convenience. The CLI JSON output remains the canonical automation interface.
