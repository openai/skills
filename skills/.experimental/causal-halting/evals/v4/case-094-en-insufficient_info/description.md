The design says a result is used but does not identify whether the current run, a later run, or an external controller consumes it.

This prose is fixture context only; deterministic scripts analyze expected.design-ir.json.
