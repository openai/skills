O resultado da observacao vai para um orquestrador, uma execucao futura, uma auditoria posterior, ou nao ha previsao modelada.

This prose is fixture context only; deterministic scripts analyze expected.design-ir.json.
