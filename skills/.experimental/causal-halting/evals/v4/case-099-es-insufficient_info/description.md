El diseno dice que se usa un resultado, pero no identifica si lo consume la ejecucion actual, una futura o un controlador externo.

This prose is fixture context only; deterministic scripts analyze expected.design-ir.json.
