interface:
  display_name: "Odoo Model Inspect"
  short_description: "Inspect Odoo model structure, fields, and views (read-only)"
  default_prompt: "Inspect the specified Odoo model and report its field definitions, record count, and view structure."

dependencies:
  tools:
    - type: "mcp"
      value: "odoo"
      description: "Odoo MCP server (odoo-simple-mcp) for read-only model inspection"
