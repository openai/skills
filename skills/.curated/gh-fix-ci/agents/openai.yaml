interface:
  display_name: "GitHub Fix CI"
  short_description: "Debug failing GitHub Actions CI"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Wait for GitHub PR checks to stabilize, inspect any failing GitHub Actions checks, summarize root cause, and propose a focused fix plan."
