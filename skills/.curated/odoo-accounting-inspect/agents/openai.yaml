interface:
  display_name: "Odoo Accounting Inspect"
  short_description: "Inspect Odoo invoices, bills, payments, and journal entries (read-only)"
  default_prompt: "Inspect the Odoo accounting data and report on invoices, payments, or journal entries as requested."

dependencies:
  tools:
    - type: "mcp"
      value: "odoo"
      description: "Odoo MCP server (odoo-simple-mcp) for read-only accounting inspection"
