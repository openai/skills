# ProjectGraph

ProjectGraph is the background structured record layer for `{{PROJECT_NAME}}`. This file was initialized by bootstrap; a real seed graph still requires read-only exploration of available project history.

## Mind Map

The interactive Markmap viewer is [PROJECT_GRAPH.html](./PROJECT_GRAPH.html). It renders from [PROJECT_GRAPH.json](./PROJECT_GRAPH.json) and [TRACE_INDEX.json](./TRACE_INDEX.json). Add audience-specific projections under `display.views` when the project needs technical, project-management, product, or other perspectives.

## Outline

### {{PROJECT_NAME}} ProjectGraph

- The ProjectGraph structure has been initialized.
- Next step: explore available project history in read-only mode and create seed traces.
- The first Graph should be reviewed by the user before it becomes the baseline for future updates.
