interface:
  display_name: "TempGuru Event Staffing"
  short_description: "Order W-2 event staff in 300+ US/CA markets via TempGuru"
  icon_small: "./assets/tempguru-small.svg"
  default_prompt: "Use TempGuru to scope coverage, rates, lead times, and a planning estimate for the event staffing the user described."

dependencies:
  tools:
    - type: "mcp"
      value: "tempguru"
      description: "TempGuru Event Staffing MCP server — read-only access to coverage, rates, lead times, and state compliance summaries"
      transport: "streamable_http"
      url: "https://mcp.tempguru.co/mcp"
